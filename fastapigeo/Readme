 Backend: API Geoespacial (FastAPI + Pydantic)

Esta API simula un servicio de datos geoespaciales conectado a una base de datos PostgreSQL/PostGIS. La comunicación con el Frontend se habilita mediante CORSMiddleware.

🛠️ Archivos Clave

main.py: Define los modelos Pydantic (para GeoJSON) y los endpoints CRUD (POST, GET All, GET One).

database.py: Simulación de la base de datos en memoria (almacenamiento temporal de puntos).

requirements.txt: Lista de dependencias.

📋 Instalación

Asegúrate de tener Python 3.8+ instalado.

Crea y activa un entorno virtual (recomendado):

python -m venv venv
.\venv\Scripts\activate  # En Windows PowerShell
# source venv/bin/activate # En Linux/macOS


Instala las dependencias necesarias:

pip install fastapi uvicorn 'python-multipart' pydantic


▶️ Ejecución de la API

Desde la carpeta /backend, ejecuta el servidor:

uvicorn main:app --reload
# La API se ejecutará en [http://127.0.0.1:8000](http://127.0.0.1:8000)
