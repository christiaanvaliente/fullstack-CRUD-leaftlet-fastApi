Frontend: React, TypeScript, Leaflet

Este proyecto es una Single Page Application (SPA) que interactúa con el Backend de FastAPI para crear y visualizar puntos geográficos.

🛠️ Archivos Clave

src/GeoCrudForm.tsx: Contiene todo el formulario, el mapa Leaflet, la lógica de la API y el manejo de estado de React.

📋 Instalación

Abre la carpeta /frontend en tu terminal.

Instala las dependencias de Node.js:

npm install


Instala los paquetes específicos de geolocalización (compatibles con React 18):

npm install leaflet react-leaflet@4.2.1
npm install --save-dev @types/leaflet@1.9.11


⚙️ Ejecución

Asegúrate de que el Backend esté corriendo en http://127.0.0.1:8000 antes de iniciar.

Desde la carpeta /frontend, ejecuta la aplicación de desarrollo de Vite:

npm run dev
# El Frontend se abrirá en http://localhost:5173



---

## 3. Paso a Paso para GitHub (Respuesta al 3er punto)

Asumiendo que ya tienes Git instalado y una cuenta de GitHub:

1.  **Inicializar Git y Estructurar:**
    * Crea la carpeta raíz: `mkdir geo-crud-fullstack`
    * Copia los archivos `frontend/` y `backend/` dentro.
    * Navega a la carpeta raíz: `cd geo-crud-fullstack`
    * Inicializa Git: `git init`

2.  **Crear Repositorio en GitHub:**
    * Ve a GitHub y crea un nuevo repositorio llamado `geo-crud-fullstack` (sin marcar la casilla "Initialize this repository with a README").

3.  **Añadir Archivos y Configuración:**
    * Asegúrate de crear un archivo `.gitignore` en la raíz con:
        ```
        # Logs
        logs
        *.log
        npm-debug.log*
        yarn-debug.log*
        yarn-error.log*
        # Dependency directories
        node_modules
        /frontend/node_modules
        /backend/venv
        .env
        ```
    * Añadir todos los archivos al seguimiento de Git: `git add .`
    * Crear la primera versión: `git commit -m "feat: Initial Fullstack Geo-CRUD setup"`

4.  **Subir a GitHub:**
    * Conecta tu repositorio local con el de GitHub (reemplaza `TU_USUARIO` por tu nombre de usuario de GitHub):
        ```bash
        git remote add origin https://github.com/TU_USUARIO/geo-crud-fullstack.git
        ```
    * Sube el código a la rama principal:
        ```bash
        git push -u origin main
